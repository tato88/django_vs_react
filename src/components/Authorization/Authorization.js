import React from 'react';


const Authorization = () => {

    const loginazi = () => {

    }
    return (
        <div>
            <input type={"button"} value={'autorization'} onClick={() => loginazi()}/>
        </div>
    );
};

export default Authorization;